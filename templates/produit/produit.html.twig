{% extends 'base.html.twig' %}

{% block title %}Hello {{ produit.nom}}!{% endblock %}

{% block body %}
<h1>{{ produit.nom }}</h1>

<div class="container">
    <div class="row">
        <div>
       <p>details: {{ produit.description }}</p>
       <p>en stock:{{ produit.stock }}</p>
        <p>prix:</p> 
           {{ produit.image }} 
            </div>
            <button></button><a href="/panier/add/{{produit.id}}">ajouter au panier</a>
        </div>
    </div>
</div>

{% endblock %}
