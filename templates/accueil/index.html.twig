{% extends 'base.html.twig' %}

{% block title %}Hello World!{% endblock %}

{% block body %}
<h1>Hello Village Green !</h1>

<div class="container">
    <div class="row">
        {% for categorie in categories %}

        <div class="card" style="width: 18rem;">
        <img src={{categorie.randomImage}} class="card-img-top" alt="...">
            <div class="card-body">
         <a href="/categorie/{{ categorie.id}}"><h5 class="card-title">{{ categorie.nom }}</h5></a>
            
    </div>
    </div>


            
        {% endfor %}
    </div> 
</div>
{% include partials/_footer.html.twig %}
{% endblock %}
