{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}

    {% for elt in panier %}
    <div>
        {{ elt.produit.nom }}
        <br>
        {{ elt.quantite }}
        </div>
        <a class="btn btn-primary" href="panier/add/{{ elt.produit.id}}">rajouter au panier</a>
        <a class="btn btn-danger" href="panier/delete/{{ elt.produit.id }}">supprimer du panier</a>
    {% endfor %}

{% endblock %}
